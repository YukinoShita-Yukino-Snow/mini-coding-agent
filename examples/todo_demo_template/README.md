# Todo CLI 示例项目

这是一个把任务保存在 JSON 文件中的小型 Python 命令行应用，目前支持添加任务、
列出任务和完成任务。

```powershell
python todo_app.py add "编写代码"
python todo_app.py list
python todo_app.py done 1
python -m pytest -q
```

该目录是公开、可复现的 Agent 功能示例基线。请使用仓库中的生成脚本复制到新目录，
不要直接修改模板。

